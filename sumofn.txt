.model small 
.stack
	printn macro p 
	lea dx,p
	mov ah,09h 
	int 21h
	endm 
	
	prints macro  reg
	mov al, reg
	mov cl,0ah
	xor ah,ah
	div cl
	mov bl,ah 
	add al,30h
	mov dl,al
	mov ah,02h
	int 21h
	add bl,30h 
	mov dl,bl 
	mov ah,02h
	int 21h
	endm
	
	
	printr macro 
	mov ah,01h
	int 21h
	sub al,30h
        
	endm 
.data 
 	m1 db  10,13,'enter limit $'
	m2 db 10,13,'enter the numbers $'
	m3 db  10,13,"the sum is $"
	arr db 10 dup(?)
	val db 0
.code 

		mov  ax, @data
		mov ds,ax
		printn m1
		printr 
		mov bl,al
		mov si,00h
		printn m2
		
	loo: 	printr               
    		mov arr[si], al      
    		inc si
    		inc val
    		cmp bl, val
    		jne loo

    		mov si, 0
    		mov val, 0
    		mov cl, 0 
	sum:
		mov al, arr[si]
    		add cl, al
    		inc si
    		inc val
    		cmp bl, val
    		jne sum

    	printn m3
    	prints cl

    	mov ah, 4Ch
    	int 21h
end
